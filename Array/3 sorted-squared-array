def sortedSquaredArray(array):
    # Write your code here.
    for i in range(0, len(array)):
        array[i] = array[i] ** 2
    return sorted(array)

parent_array = [1, 2, 3, 5, 6, 8, 9]
print(sortedSquaredArray(parent_array))